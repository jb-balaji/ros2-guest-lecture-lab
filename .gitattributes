# Normalize all text to LF in the repo so shell scripts and ROS files
# work on Linux / The Construct regardless of the OS they were committed from.
* text=auto eol=lf

# Binary assets must never be line-ending converted
*.pgm binary
*.png binary
*.jpg binary
*.jpeg binary
